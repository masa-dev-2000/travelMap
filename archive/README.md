# archive

現行の TravelMap (cloudflare/) では使っていないファイル。2026-09-18 に移動。

- `firebase-app/` 旧 Firebase 版 (地図 `travelMap/`、入力 `inputTravelLog/`、Hosting 設定、移行・更新スクリプト)。Firebase 上の旧サイトは停止していない。再デプロイする場合はこのフォルダを作業ディレクトリにする。
- `design/` UI 検討時のモック (入力10案、地図案)。
