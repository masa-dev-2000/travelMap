# 入力フロー比較モック

ローカル確認: `python -m http.server 8795 --bind 127.0.0.1` をこのディレクトリで起動。

- A: `http://127.0.0.1:8795/?mode=a`。行動・場所・日時・旅 → メモ・評価 → 支出額・分類。
- B: `http://127.0.0.1:8795/?mode=b`。同じ8項目を1項目ずつ入力。支出なしなら分類を省略。
- 戻る際の値保持、任意項目スキップ、入力チェック、完了表示あり。両案は別々に入力を保持し、再読み込みで初期化。
- 日時・旅・分類・現在地は比較用サンプル。現在地ボタンは位置情報を取得しない。API通信・実データ保存なし。本体未反映。
- 2026-09-09: 390×844でページ・入力領域にスクロールが出ない初期表示、Aの値保持・支出保存、Bのスキップ・支出なし完了をブラウザ確認。実機キーボードは未確認。画面が低い場合は入力領域のみスクロール可能にして操作ボタンを固定。
