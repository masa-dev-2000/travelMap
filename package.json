{
  "scripts": {
    "test": "npm --prefix cloudflare test",
    "check": "npm --prefix cloudflare run check",
    "test:model": "python -m unittest discover -s data_model -v"
  },
  "dependencies": {
    "firebase-admin": "^13.6.0"
  },
  "devDependencies": {
    "eslint": "^8.57.1"
  }
}
